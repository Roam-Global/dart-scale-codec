name: scalecodec_demo
description: Usage demo of scalecodec lib
publish_to: 'none'

dependencies:
  convert: ^3.0.1
  scalecodec:
    path: ../

dev_dependencies:
  build_runner: any

environment:
  sdk: '>=2.10.0 <3.0.0'
